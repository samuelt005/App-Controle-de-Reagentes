export interface FiltersValue {
  search?: string | null,
  un: string | null,
  tag: string | null,
  qtdMin: number | null,
  qtdMax: number | null,
  vlrUnitMin: number | null,
  vlrUnitMax: number | null,
  vlrTotMin: number | null,
  vlrTotMax: number | null,
  loc: string | null,
}
